import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand Colors
  static const Color mrfRed       = Color(0xFFCC0000);   // MRF signature red
  static const Color mrfDarkRed   = Color(0xFF990000);   // Darker red for pressed states
  static const Color mrfBlack     = Color(0xFF1A1A1A);   // Near-black for surfaces
  static const Color mrfDarkGrey  = Color(0xFF2C2C2C);   // Cards & containers
  static const Color mrfMidGrey   = Color(0xFF5A5A5A);   // Secondary text
  static const Color mrfLightGrey = Color(0xFFF2F2F2);   // Backgrounds
  static const Color mrfWhite     = Color(0xFFFFFFFF);   // Clean white
  static const Color mrfGold      = Color(0xFFFFC107);   // Accent / badges / ratings
  static const Color mrfGreen     = Color(0xFF2E7D32);   // Success / active status
  static const Color mrfOrange    = Color(0xFFE65100);   // Warning / upcoming due
}
